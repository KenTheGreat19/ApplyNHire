{
  "scripts": {
    "setup": "npm install && npx prisma generate && echo 'Setup complete! Now configure your .env file and run: npx prisma db push'",
    "dev": "next dev",
    "build": "prisma generate && next build",
    "start": "next start",
    "lint": "next lint",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "postinstall": "prisma generate"
  }
}
